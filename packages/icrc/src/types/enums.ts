export enum AccountDefaultEnum {
    Default
}

export enum TransactionTypeEnum { NONE = "NONE", RECEIVE = "RECEIVE", SEND = "SEND" };

export enum OperationStatusEnum { COMPLETED = "COMPLETED", REVERTED = "REVERTED", PENDING = "PENDING" };

export enum OperationTypeEnum { TRANSACTION = "TRANSACTION", FEE = "FEE" };
